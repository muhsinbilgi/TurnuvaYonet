
@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

@model IEnumerable<VeritabaniKatmani.Sporcular>

<div class="breadcrumbs">
    <div class="col-sm-4">
        <div class="page-header float-left">
            <div class="page-title">
                <h1>Takımlar</h1>
            </div>
        </div>
    </div>
    <div class="col-sm-8">
        <div class="page-header float-right">
            <div class="page-title">
                <ol class="breadcrumb text-right">
                    <li class="active">@Html.ActionLink("Sporcu Ekle", "Ekle", "Sporcu", new { @class = "btn btn-success" })</li>
                </ol>
            </div>
        </div>
    </div>
</div>

<div class="col-lg-6">
    <div class="card">

        <div class="card-body">
            <table class="table table-striped">
                <thead>
                    <tr>
                        <th scope="col"></th>
                        <th scope="col"> @Html.DisplayNameFor(model => model.Adi) @Html.DisplayNameFor(model => model.Soyadi)</th>
                        <th scope="col">Takım Adı</th>
                        <th scope="col"> Doğum Yeri</th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                        <th scope="col"></th>
                    </tr>
                </thead>

                @foreach (var item in Model)
                {
                    <tbody>
                        <tr>
                            <td><img src="~/Image/@Html.DisplayFor(modelItem => item.Resim)" width="25" height="25" /></td>
                            <td>@Html.DisplayFor(modelItem => item.Adi) @Html.DisplayFor(modelItem => item.Soyadi)</td>
                            <td>@Html.DisplayFor(modelItem => item.TakimAdi)</td>
                            <td>@Html.DisplayFor(modelItem => item.DogumYeri)</td>
                            <td>@Html.ActionLink("Detay", "Detay", "Sporcular", new { id = item.Id }, new { @class = "btn btn-secondary" })</td>
                            <td>@Html.ActionLink("Düzenle", "Duzenle", "Sporcular", new { id = item.Id }, new { @class = "btn btn-primary" })</td>
                            <td>
                                @using (Html.BeginForm("Sil", "Sporcular", new { Id = item.Id }, FormMethod.Post))
                                {
                                    <button class="btn btn-danger">Sil</button>
                                }

                            </td>


                        </tr>

                    </tbody>
                }

            </table>
        </div>
    </div>
</div>

